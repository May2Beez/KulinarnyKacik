import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Kulinarny Kacik Ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
